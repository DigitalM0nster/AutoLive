# Список задач по доработке проекта AutoLive

## 🔴 Критический приоритет

### ✅ 1. Исправить создание бронирования с клиентской формы
**Статус:** ✅ Выполнено  
**Дата начала:** 2024-12-XX
**Дата завершения:** 2025-01-XX

**Проблема:**
- Форма отправляет на `/api/booking`, но API находится по адресу `/api/bookings` (с 's')
- Формат данных не соответствует ожидаемому API
- `managerId` обязателен в API, но клиент не может его выбрать
- Нет выбора отдела

**Что было сделано:**
1. ✅ Добавить `bookingDepartmentId` в модель Booking (миграция) - было выполнено ранее
2. ✅ Сделать `managerId` опциональным в модели Booking - было выполнено ранее
3. ✅ Создать публичный API `/api/bookings/public` - создан endpoint для создания бронирования без авторизации
4. ✅ Создать публичный API `/api/booking-departments/public` - создан endpoint для получения списка отделов без авторизации
5. ✅ Обновить форму: добавить выбор отдела - форма теперь загружает и отображает отделы
6. ✅ Использовать `PhoneInput` для телефона - заменён обычный input на компонент PhoneInput
7. ✅ Заменить `alert()` на toast-уведомления - используется showSuccessToast/showErrorToast
8. ✅ Исправить URL API в форме - форма отправляет на `/api/bookings/public`
9. ✅ Исправить формат данных - данные отправляются в правильном формате (scheduledDate, scheduledTime, bookingDepartmentId, contactPhone, clientName, notes)
10. ✅ Добавить ToastProvider в layout клиентской части

**Файлы, которые были изменены:**
- ✅ `src/app/api/bookings/public/route.ts` - создан новый публичный endpoint
- ✅ `src/app/api/booking-departments/public/route.ts` - создан публичный endpoint для получения отделов
- ✅ `src/app/(site)/booking/ServiceBookingContent.tsx` - полностью обновлена форма
- ✅ `src/app/(site)/layout.tsx` - добавлен ToastProvider

---

### ✅ 2. Заменить `new PrismaClient()` на импорт
**Статус:** ✅ Выполнено  
**Дата начала:** 2024-12-XX
**Дата завершения:** 2025-01-XX

**Проблема:** В некоторых файлах создается новый экземпляр `PrismaClient` вместо использования единого из `@/lib/prisma`.

**Проверка:**
- ✅ Проверен файл `src/app/api/bookings/[id]/route.ts` - использует правильный импорт `import { prisma } from "@/lib/prisma";`
- ✅ Проверены все файлы в `src/app/api` - везде используется импорт из `@/lib/prisma`
- ✅ Единственное место создания `PrismaClient` - это `src/lib/prisma.ts`, где создаётся единый экземпляр (это правильно)

**Вывод:** Задача уже была выполнена ранее. Все файлы используют единый экземпляр `prisma` из `@/lib/prisma`.

---

## 🟡 Средний приоритет

### 3. Обработка удалённых товаров в заказах (логи)
**Статус:** Запланировано

**Проблема:** 
- Если товар удалён из базы, но он есть в заказе, нужно понять, как он будет отображаться в логах заказа
- Важно сохранить информацию о товаре в логах даже после его удаления

**Что нужно сделать:**
1. Проверить текущее поведение системы при удалении товара, который есть в заказе
2. Определить стратегию: сохранять данные товара в логах или использовать связь с удалённым товаром
3. Реализовать корректное отображение удалённых товаров в логах заказов
4. Добавить маркер "Товар удалён" в интерфейс, если товар больше не существует

**Файлы для проверки/изменения:**
- `src/app/api/orders/[id]/route.ts` - логика получения заказа
- `src/app/admin/orders/` - компоненты отображения заказов
- `prisma/schema.prisma` - возможно, нужны изменения в модели

---

### 4. Обработка удаления адреса (логи заказов и записей)
**Статус:** Запланировано

**Проблема:**
- Если адрес удалён, но его используют заказы или записи (бронирования), нужно определить поведение системы
- Что будет записано в логах при удалении адреса, который связан с активными заказами/записями

**Что нужно сделать:**
1. Проверить текущую логику удаления адресов
2. Определить поведение: блокировать удаление, если адрес используется, или сохранять данные в логах
3. Реализовать корректную обработку связанных заказов и записей
4. Обеспечить отображение информации об удалённом адресе в логах

**Файлы для проверки/изменения:**
- `src/app/api/addresses/[id]/route.ts` - логика удаления адреса
- Логирование должно фиксировать удаление и связанные объекты

---

### 5. Обработка удаления записи (бронирования) - связанные заказы
**Статус:** Запланировано

**Проблема:**
- Если запись (бронирование) удалена, но с ней связан заказ, нужно определить поведение системы
- Что будет отображаться в логах заказа, если связанная запись была удалена

**Что нужно сделать:**
1. Проверить связь между записями (бронированиями) и заказами в базе данных
2. Определить стратегию: блокировать удаление или сохранять информацию в логах
3. Реализовать корректное отображение удалённой записи в логах заказа
4. Добавить маркер "Запись удалена" в интерфейс заказа

**Файлы для проверки/изменения:**
- `src/app/api/bookings/[id]/route.ts` - логика удаления записи
- `src/app/api/orders/[id]/route.ts` - отображение связанных записей
- `prisma/schema.prisma` - проверить связи между моделями

---

### 6. Реализовать полноценную систему логирования для записей и заказов с перекрёстными связями
**Статус:** Запланировано

**Проблема:**
- Логи должны сохранять полные данные даже если связанные сущности удаляются с сайта
- Нужно реализовать перекрёстное логирование между связанными сущностями (адрес → запись → заказ)
- При изменении/удалении адреса отдела записи это должно отражаться во всех связанных логах
- При изменении/удалении записи это должно отражаться в логах связанного заказа
- Логи должны содержать достаточно информации для полного понимания что и кто делал

**Что нужно сделать:**

1. **Улучшить сохранение данных в логах:**
   - При создании/изменении записи сохранять полный снапшот всех связанных данных (адрес отдела, данные клиента, менеджера)
   - При создании/изменении заказа сохранять полный снапшот всех связанных данных (адрес, связанная запись, товары)
   - Сохранять данные в JSON-полях логов так, чтобы даже при удалении связанных сущностей данные оставались в логах

2. **Реализовать перекрёстное логирование адреса отдела записи:**
   - При изменении адреса в `BookingDepartment`:
     - Создать запись в `BookingDepartmentLog` (логирование изменения адреса)
     - Найти все записи (`Booking`), связанные с этим отделом, и создать записи в `BookingLog` с информацией о том, что был изменён адрес отдела
     - Для каждой записи найти связанный заказ (`Order`), если есть, и создать запись в `OrderLog` с информацией о том, что был изменён адрес из-за изменения адреса отдела записи
   - При удалении адреса (если это будет разрешено):
     - Аналогично логировать во всех связанных местах, указав что адрес был удалён и какие данные были связаны с этим адресом

3. **Реализовать перекрёстное логирование записи:**
   - При изменении записи (`Booking`):
     - Если изменяется адрес через изменение отдела, логировать как в пункте 2
     - Если есть связанный заказ (`Order`), создать запись в `OrderLog` о том, что связанная запись была изменена (с сохранением снапшота записи до и после изменения)
   - При удалении записи:
     - Если есть связанный заказ, создать запись в `OrderLog` о том, что связанная запись была удалена (с сохранением полного снапшота удалённой записи)

4. **Реализовать перекрёстное логирование заказа:**
   - При изменении заказа, если есть связанная запись, опционально логировать изменения в `BookingLog`
   - Сохранять полные снапшоты связанных сущностей во всех операциях

5. **Улучшить отображение логов:**
   - В интерфейсе просмотра логов заказа показывать связанные изменения в записях
   - В интерфейсе просмотра логов записи показывать связанные изменения в адресах отдела
   - Показывать индикаторы о том, что связанные сущности были удалены (если снапшот указывает на удалённую сущность)

**Примеры логирования:**

- **Изменение адреса отдела:**
  - `BookingDepartmentLog`: "Адрес отдела изменён с 'ул. Примерная, 1' на 'ул. Новая, 2'"
  - `BookingLog` (для каждой связанной записи): "Изменён адрес отдела записи с 'ул. Примерная, 1' на 'ул. Новая, 2' (отдел: Название отдела, ID: 123)"
  - `OrderLog` (для заказа, связанного с записью): "Изменён адрес: был адрес отдела записи 'ул. Примерная, 1' (отдел: Название отдела, ID: 123), теперь 'ул. Новая, 2', так как был изменён адрес отдела записи"

- **Удаление записи с адресом:**
  - `BookingLog`: "Запись удалена. Была связана с заказом ID: 456, адрес отдела: 'ул. Примерная, 1'"
  - `OrderLog`: "Связанная запись (ID: 789) была удалена. Адрес отдела записи был: 'ул. Примерная, 1' (отдел: Название отдела, ID: 123)"

**Файлы для создания/изменения:**
- `src/app/api/booking-departments/[id]/route.ts` - добавить перекрёстное логирование при изменении/удалении
- `src/app/api/bookings/[id]/route.ts` - улучшить логирование с полными снапшотами, добавить перекрёстное логирование
- `src/app/api/orders/[id]/route.ts` - улучшить логирование с полными снапшотами, добавить перекрёстное логирование
- `src/lib/universalLogging.ts` - возможно, создать утилиты для перекрёстного логирования
- `prisma/schema.prisma` - проверить, достаточно ли JSON-полей для хранения снапшотов (скорее всего достаточно)
- Компоненты отображения логов в админ-панели - улучшить отображение связанных изменений

**Важно:**
- Все данные должны сохраняться в снапшотах так, чтобы даже при удалении связанных сущностей логи оставались информативными
- Снапшоты должны содержать достаточную информацию: ID, название, адрес (если есть), название отдела и т.д.
- Перекрёстное логирование должно объяснять причину изменений (например, "адрес изменился потому что был изменён адрес отдела записи")

---

### 7. Подключение API каталога запчастей
**Статус:** Запланировано

**Описание:** 
- Необходимо найти и подключить API с каталогом запчастей от сторонних поставщиков
- Нужно исследовать доступные каталоги запчастей и их API

**Что нужно сделать:**
1. Исследовать доступные каталоги запчастей и их API (например, AutoDoc, Exist и т.д.)
2. Выбрать подходящий каталог/каталоги
3. Изучить документацию API и требования к интеграции
4. Создать сервис для работы с внешним API
5. Реализовать синхронизацию данных с каталогом
6. Добавить функционал поиска товаров из внешнего каталога

**Файлы для создания/изменения:**
- `src/lib/catalog-api/` - новый сервис для работы с API каталога
- Возможно, новые таблицы в БД для хранения синхронизированных данных

---

### 8. Подключение онлайн видео-общения
**Статус:** Запланировано

**Описание:**
- Необходимо добавить функционал онлайн видео-общения между клиентом и администраторами
- Должно быть максимально удобно для пользователя (простое подключение, без установки дополнительного ПО)

**Что нужно сделать:**
1. Исследовать доступные решения (WebRTC, готовые сервисы типа Twilio Video, Jitsi Meet и т.д.)
2. Выбрать оптимальное решение (предпочтительно встроенное в браузер, без установки)
3. Реализовать интеграцию на клиентской части
4. Добавить панель управления видео-звонками в админ-панель
5. Создать систему уведомлений для администраторов о входящих звонках
6. Добавить возможность записи звонков (опционально)

**Файлы для создания/изменения:**
- `src/components/video-call/` - компоненты видео-общения
- `src/app/api/video-call/` - API для управления звонками
- `src/app/admin/video-calls/` - админ-панель для видео-звонков

---

### 9. Создать личный кабинет клиента
**Статус:** Запланировано

**Что нужно:**
- Страница `/profile` или `/account` для авторизованных клиентов
- Отображение списка заказов клиента
- Отображение списка записей (бронирований) клиента
- Возможность редактирования профиля (имя, фамилия, отчество)

**Файлы для создания:**
- `src/app/(site)/profile/page.tsx`
- `src/app/(site)/profile/ProfileContent.tsx`
- API endpoints для получения заказов и записей клиента

---

### 10. Заменить `alert()` на toast-уведомления
**Статус:** Запланировано

**Где используется:**
- ✅ `src/app/(site)/booking/ServiceBookingContent.tsx` - форма бронирования (уже исправлено)
- Другие места (нужно проверить)

---

### 11. Улучшить структуру для незарегистрированных клиентов
**Статус:** Запланировано

**Что нужно:** Сохранять имя и телефон незарегистрированных клиентов в структурированном виде в `notes`.

---

## 🟢 Низкий приоритет (можно доработать позже)

### 12. Проверка занятых слотов при бронировании
**Статус:** Можно доработать

**Описание:** Добавить проверку, не занято ли выбранное время перед созданием записи.  
**Примечание:** Не критично, так как все заявки обрабатываются менеджером вручную, менеджер ведёт учёт заявок НЕ на сайте.

---

### 13. Завершить миграцию
**Статус:** Запланировано

**Что нужно:** Удалить поля с TODO комментариями из `prisma/schema.prisma` после завершения миграции данных.

---

## 📝 Заметки

- Все заявки на бронирование обрабатываются менеджерами вручную
- Менеджеры ведут учёт заявок НЕ на сайте
- Клиент выбирает отдел, заявка попадает в общие заявки отдела без назначенного менеджера
- Менеджеры потом берут заявки себе и присваивают их себе