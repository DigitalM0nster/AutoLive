# Список задач по доработке проекта AutoLive

## ✅ Выполненные задачи

### ✅ 1. Исправить создание бронирования с клиентской формы
**Статус:** ✅ Выполнено  
**Дата начала:** 2024-12-XX
**Дата завершения:** 2025-01-XX

**Проблема:**
- Форма отправляет на `/api/booking`, но API находится по адресу `/api/bookings` (с 's')
- Формат данных не соответствует ожидаемому API
- `managerId` обязателен в API, но клиент не может его выбрать
- Нет выбора отдела

**Что было сделано:**
1. ✅ Добавить `bookingDepartmentId` в модель Booking (миграция) - было выполнено ранее
2. ✅ Сделать `managerId` опциональным в модели Booking - было выполнено ранее
3. ✅ Создать публичный API `/api/bookings/public` - создан endpoint для создания бронирования без авторизации
4. ✅ Создать публичный API `/api/booking-departments/public` - создан endpoint для получения списка отделов без авторизации
5. ✅ Обновить форму: добавить выбор отдела - форма теперь загружает и отображает отделы
6. ✅ Использовать `PhoneInput` для телефона - заменён обычный input на компонент PhoneInput
7. ✅ Заменить `alert()` на toast-уведомления - используется showSuccessToast/showErrorToast
8. ✅ Исправить URL API в форме - форма отправляет на `/api/bookings/public`
9. ✅ Исправить формат данных - данные отправляются в правильном формате (scheduledDate, scheduledTime, bookingDepartmentId, contactPhone, clientName, notes)
10. ✅ Добавить ToastProvider в layout клиентской части

**Файлы, которые были изменены:**
- ✅ `src/app/api/bookings/public/route.ts` - создан новый публичный endpoint
- ✅ `src/app/api/booking-departments/public/route.ts` - создан публичный endpoint для получения отделов
- ✅ `src/app/(site)/booking/ServiceBookingContent.tsx` - полностью обновлена форма
- ✅ `src/app/(site)/layout.tsx` - добавлен ToastProvider

---

### ✅ 2. Заменить `new PrismaClient()` на импорт
**Статус:** ✅ Выполнено  
**Дата начала:** 2024-12-XX
**Дата завершения:** 2025-01-XX

**Проблема:** В некоторых файлах создается новый экземпляр `PrismaClient` вместо использования единого из `@/lib/prisma`.

**Проверка:**
- ✅ Проверен файл `src/app/api/bookings/[id]/route.ts` - использует правильный импорт `import { prisma } from "@/lib/prisma";`
- ✅ Проверены все файлы в `src/app/api` - везде используется импорт из `@/lib/prisma`
- ✅ Единственное место создания `PrismaClient` - это `src/lib/prisma.ts`, где создаётся единый экземпляр (это правильно)

**Вывод:** Задача уже была выполнена ранее. Все файлы используют единый экземпляр `prisma` из `@/lib/prisma`.

---

### ✅ 3. Обработка удалённых товаров в заказах (логи)
**Статус:** ✅ Выполнено

**Решение:**
- Товары в заказах сохраняются в таблице `OrderItem` с полными данными (название, SKU, цена, бренд, изображение)
- При удалении товара из базы данные товара остаются в заказе, так как они хранятся в `OrderItem`
- В логах заказов сохраняются полные снапшоты заказа, включая все товары из `orderItems`
- Данные товаров сохраняются в снапшотах логов, поэтому информация не теряется даже при удалении товара
- **Важно:** Логи также сохраняются в общую таблицу `ChangeLog`, поэтому информация остаётся даже если специализированные логи удалятся
- **Важно:** При удалении товара лог создаётся ДО удаления (в транзакции), чтобы он не удалился из-за Cascade

**Что было сделано:**
- Улучшены снапшоты заказов в логах - теперь включают полную информацию о товарах
- Создана функция `getFullOrderSnapshot()` для получения полного снапшота заказа со всеми связанными данными
- Добавлено логирование в общую таблицу `ChangeLog` при создании и изменении заказов
- Исправлено логирование при удалении товаров - теперь лог создаётся ДО удаления в транзакции
- Добавлено логирование в ChangeLog при удалении товаров (одиночное и массовое)

**Файлы, которые были изменены:**
- ✅ `src/lib/crossLogging.ts` - создана функция `getFullOrderSnapshot()`
- ✅ `src/app/api/orders/[orderId]/route.ts` - улучшены снапшоты в логах заказов, добавлено логирование в ChangeLog
- ✅ `src/app/api/orders/route.ts` - добавлено логирование в ChangeLog при создании заказа
- ✅ `src/app/api/products/[productId]/route.ts` - исправлено логирование при удалении товара (теперь ДО удаления), добавлено логирование в ChangeLog
- ✅ `src/app/api/products/bulk-delete/route.ts` - исправлено логирование при массовом удалении товаров (теперь ДО удаления), добавлено логирование в ChangeLog

---

### ✅ 4. Обработка удаления адреса (логи заказов и записей)
**Статус:** ✅ Выполнено

**Решение:**
- Удаление адреса отдела записи блокируется, если есть связанные записи (бронирования)
- При изменении адреса выполняется перекрёстное логирование во всех связанных записях и заказах
- Данные адреса сохраняются в снапшотах логов, поэтому информация не теряется

**Что было сделано:**
- Реализовано перекрёстное логирование при изменении адреса отдела записи
- При изменении адреса создаются логи во всех связанных записях (`BookingLog`) и заказах (`OrderLog`)
- Улучшены сообщения в логах - теперь показывают детальную информацию об изменениях

**Файлы, которые были изменены:**
- ✅ `src/lib/crossLogging.ts` - создана функция `logBookingDepartmentChangeCrossLogging()`
- ✅ `src/app/api/booking-departments/[id]/route.ts` - добавлено перекрёстное логирование при изменении адреса

---

### ✅ 5. Обработка удаления записи (бронирования) - связанные заказы
**Статус:** ✅ Выполнено

**Решение:**
- При удалении записи, если есть связанный заказ, создаётся лог в `OrderLog` с полным снапшотом удалённой записи
- В логе сохраняется информация об адресе отдела записи, чтобы данные не терялись
- При изменении записи также создаётся перекрёстный лог в связанном заказе
- **Важно:** Лог удаления записи создаётся ДО удаления записи (в транзакции), чтобы он не удалился из-за `onDelete: Cascade`
- Логи также сохраняются в общую таблицу `ChangeLog`, поэтому информация остаётся даже если запись удалена

**Что было сделано:**
- Реализовано перекрёстное логирование при удалении записи
- Реализовано перекрёстное логирование при изменении записи
- В логах сохраняются полные снапшоты удалённых записей со всеми связанными данными
- Исправлена проблема с удалением логов при удалении записи - теперь лог создаётся ДО удаления
- Добавлено логирование в общую таблицу `ChangeLog` при создании, изменении и удалении записей

**Файлы, которые были изменены:**
- ✅ `src/lib/crossLogging.ts` - созданы функции `logBookingDeleteCrossLogging()` и `logBookingChangeCrossLogging()`
- ✅ `src/app/api/bookings/[id]/route.ts` - добавлено перекрёстное логирование, исправлено создание лога при удалении (теперь ДО удаления), добавлено логирование в ChangeLog
- ✅ `src/app/api/bookings/route.ts` - добавлено логирование в ChangeLog при создании записи

---

### ✅ 6. Реализовать полноценную систему логирования для записей и заказов с перекрёстными связями
**Статус:** ✅ Выполнено

**Что было сделано:**
1. ✅ **Улучшено сохранение данных в логах:**
   - Созданы функции `getFullOrderSnapshot()` и `getFullBookingSnapshot()` для получения полных снапшотов
   - При создании/изменении записи сохраняются полные снапшоты всех связанных данных (адрес отдела, данные клиента, менеджера)
   - При создании/изменении заказа сохраняются полные снапшоты всех связанных данных (адрес, связанная запись, товары)
   - Все данные сохраняются в JSON-полях логов, поэтому информация не теряется даже при удалении связанных сущностей

2. ✅ **Реализовано перекрёстное логирование адреса отдела записи:**
   - При изменении адреса в `BookingDepartment` создаётся запись в `BookingDepartmentLog`
   - Находятся все записи (`Booking`), связанные с этим отделом, и создаются записи в `BookingLog`
   - Для каждой записи находится связанный заказ (`Order`), если есть, и создаётся запись в `OrderLog`
   - Все логи содержат детальную информацию об изменениях

3. ✅ **Реализовано перекрёстное логирование записи:**
   - При изменении записи (`Booking`) создаётся запись в `OrderLog` связанного заказа (если есть)
   - Сохраняются снапшоты записи до и после изменения
   - При удалении записи создаётся запись в `OrderLog` с полным снапшотом удалённой записи

4. ✅ **Реализовано перекрёстное логирование заказа:**
   - При изменении заказа сохраняются полные снапшоты всех связанных сущностей
   - Улучшены снапшоты заказов - теперь включают менеджера, адрес доставки и другую информацию

**Файлы, которые были созданы/изменены:**
- ✅ `src/lib/crossLogging.ts` - создан новый файл с утилитами для перекрёстного логирования
- ✅ `src/app/api/booking-departments/[id]/route.ts` - добавлено перекрёстное логирование при изменении адреса, добавлено логирование в ChangeLog
- ✅ `src/app/api/bookings/[id]/route.ts` - улучшено логирование с полными снапшотами, добавлено перекрёстное логирование, исправлено создание лога при удалении (теперь ДО удаления), добавлено логирование в ChangeLog
- ✅ `src/app/api/bookings/route.ts` - добавлено логирование в ChangeLog при создании записи
- ✅ `src/app/api/orders/[orderId]/route.ts` - улучшено логирование с полными снапшотами, добавлено логирование в ChangeLog
- ✅ `src/app/api/orders/route.ts` - добавлено логирование в ChangeLog при создании заказа
- ✅ `src/app/api/products/[productId]/route.ts` - исправлено логирование при удалении товара (теперь ДО удаления), добавлено логирование в ChangeLog
- ✅ `src/app/api/products/bulk-delete/route.ts` - исправлено логирование при массовом удалении товаров (теперь ДО удаления), добавлено логирование в ChangeLog
- ✅ `src/app/api/categories/[categoryId]/route.ts` - добавлено логирование в ChangeLog при удалении и изменении категории
- ✅ `src/app/api/categories/route.ts` - добавлено логирование в ChangeLog при создании категории
- ✅ `src/app/api/service-kits/[kitId]/route.ts` - добавлено логирование в ChangeLog при удалении и изменении комплекта ТО
- ✅ `src/app/api/service-kits/route.ts` - добавлено логирование в ChangeLog при создании комплекта ТО
- ✅ `src/app/api/booking-departments/route.ts` - добавлено логирование в ChangeLog при создании адреса отдела записи

---

### ✅ 10. Заменить `alert()` на toast-уведомления
**Статус:** ✅ Выполнено

**Где использовалось:**
- ✅ `src/app/(site)/booking/ServiceBookingContent.tsx` - форма бронирования (уже было исправлено ранее)
- ✅ `src/app/admin/bookings/local_components/allBookingsLogs/AllBookingsLogsComponent.tsx` - логи записей
- ✅ `src/app/admin/categories/local_components/CategoryPageClient.tsx` - удаление категории
- ✅ `src/app/admin/product-management/products/local_components/allProductsLogs/AllProductsLogsComponent.tsx` - логи продуктов
- ✅ `src/app/admin/product-management/products/local_components/allProducts/AllProductsTable.tsx` - управление товарами
- ✅ `src/components/user/loginPopup/PasswordResetForm.tsx` - восстановление пароля
- ✅ `src/app/admin/reset-password/page.tsx` - восстановление пароля админа
- ✅ `src/app/admin/departments/local_components/allDepartmentsLogs/AllDepartmentsLogsComponent.tsx` - логи отделов
- ✅ `src/app/admin/departments/local_components/department/DepartmentPageClient.tsx` - удаление отдела
- ✅ `src/app/admin/users/local_components/allUsersLogs/AllUsersLogsComponent.tsx` - логи пользователей
- ✅ `src/app/(site)/register/RegisterContent.tsx` - регистрация

**Что было сделано:**
- Все `alert()` заменены на соответствующие toast-уведомления (`showSuccessToast`, `showErrorToast`, `showWarningToast`)
- Добавлены необходимые импорты из `@/components/ui/toast/ToastProvider`
- ToastProvider уже подключен в админ-панели и клиентской части

---

### ✅ 11. Улучшить структуру для незарегистрированных клиентов
**Статус:** ✅ Выполнено

**Что было сделано:** 
- Улучшен формат сохранения данных незарегистрированных клиентов в заказах
- Теперь данные сохраняются в структурированном виде в поле `comments` (массив строк)
- Используется единообразный формат, аналогичный бронированиям:
  ```
  --- Данные незарегистрированного клиента ---
  Имя: [имя клиента]
  Телефон: [телефон клиента]
  ---
  ```

**Файлы, которые были изменены:**
- ✅ `src/app/api/orders/public/route.ts` - обновлен формат сохранения данных незарегистрированного клиента

**Примечание:** 
- В бронированиях данные незарегистрированных клиентов уже сохранялись в структурированном виде в поле `notes` (строка)
- В заказах теперь используется аналогичный формат в поле `comments` (массив строк) для единообразия

---

## 📋 Задачи для выполнения

### 1. Сделать дизайн внутри редактора контента сайта
**Статус:** Запланировано

**Описание:** 
В разделе `/admin/content/` есть страницы для редактирования контента сайта (главная страница, контакты, акции), но внутри редакторов контента дизайн не реализован - там только заглушки с текстом.

**Что нужно сделать:**
1. Реализовать полноценный дизайн и функционал редактора контента для главной страницы (`/admin/content/homepage`)
2. Реализовать полноценный дизайн и функционал редактора контента для страницы контактов (`/admin/content/contacts`)
3. Реализовать полноценный дизайн и функционал редактора контента для страницы акций (`/admin/content/promotions`)
4. Добавить WYSIWYG редактор или текстовый редактор для редактирования контента
5. Добавить возможность загрузки изображений, редактирования текста, форматирования и т.д.
6. Стилизовать интерфейс редактора в соответствии с общим дизайном админ-панели

**Файлы для создания/изменения:**
- `src/app/admin/content/homepage/page.tsx` - сейчас только заглушка
- `src/app/admin/content/contacts/page.tsx` - сейчас только заглушка
- `src/app/admin/content/promotions/` - страницы редактирования акций
- Возможно, новые компоненты для редактора контента в `src/components/admin/content-editor/` или `src/app/admin/content/local_components/`

---

### ✅ 2. Завершить миграцию
**Статус:** ✅ Выполнено

**Что было сделано:**
1. Удалены из `prisma/schema.prisma` все поля с TODO «удалить после миграции»:
   - **bulk_action_log:** user_id, department_id, snapshots
   - **import_log:** userId, departmentId, snapshots, snapshotBefore, snapshotAfter
   - **product_log:** userId, departmentId, productId, snapshotBefore, snapshotAfter (заменены на productSnapshotBefore, productSnapshotAfter в формате Json)
   - **user_log:** admin_id, target_user_id, department_id, user_id, snapshot_before, snapshot_after
2. Обновлена запись логов: bulk-delete, import-chunk, product API, universalLogging пишут только в актуальные Json-поля.
3. Обновлено чтение логов: products/logs, product [id]/logs, [importLogId]/products — используют productSnapshotBefore/productSnapshotAfter и фильтр по departmentSnapshot (Json).
4. Добавлена миграция `20260206120000_remove_todo_log_fields`.

**Что нужно сделать с твоей стороны:** выполнить `npx prisma migrate deploy` (или `prisma migrate dev`) и при необходимости `npx prisma generate`.

---

### ✅ 2. Проверка логов у всех объектов на сайте
**Статус:** ✅ Выполнено

**Что было сделано:**
1. **BookingDepartments (адреса отделов записей):**
   - Добавлена страница логов конкретного адреса: `/admin/booking-departments/[id]/logs`
   - Компонент `BookingDepartmentLogsContent` (фильтр по `bookingDepartmentId` через существующий API)
   - На странице редактирования адреса добавлена вкладка «История изменений»

2. **ServiceKits (комплекты ТО):**
   - Добавлена страница логов конкретного комплекта: `/admin/product-management/kits/[kitId]/logs`
   - Компонент `ServiceKitLogsContent` (фильтр по `serviceKitId` через существующий API)
   - Ссылка «История изменений комплекта ТО» на странице комплекта ведёт на эту страницу

3. **Categories (категории):**
   - Добавлен общий список логов: `/admin/categories/logs` и API `GET /api/categories/logs` (данные из ChangeLog, `entityType: "category"`)
   - Добавлены логи конкретной категории: `/admin/categories/[id]/logs` и API `GET /api/categories/[categoryId]/logs`
   - Компонент `CategoryLogsContent` (общий список и по categoryId)
   - На главной странице категорий добавлена вкладка «Логи категорий», на странице категории — «История изменений»

**Итог по объектам:**
- Users, Departments, Products, Orders, Bookings — всё уже было
- BookingDepartments — ✅ добавлены логи конкретного объекта
- ServiceKits — ✅ добавлены логи конкретного объекта
- Categories — ✅ добавлены общий список логов и логи по объекту (из ChangeLog)

**Что осталось (по пункту 4):** улучшить визуальное отображение удалённых сущностей в логах — см. задачу 3

**Проверка качества логов (фильтры, карточки «Кем выполнено», раскрываемые блоки):**
- ✅ **Проверены и доработаны:** логи категорий, логи отделов, логи пользователей.
- ⏳ **Осталось проверить и при необходимости привести к тому же уровню:** логи записей (bookings), логи заказов (orders), логи товаров (products), логи адресов записей (booking-departments), логи комплектов ТО (kits).

---

### 3. Улучшить визуальное отображение удаленных сущностей в логах
**Статус:** Запланировано

**Описание:** 
Улучшить визуальное отображение удаленных сущностей в существующих логах всех объектов.

**Что нужно сделать:**
1. Добавить иконку удаления (красный крестик или мусорка)
2. Улучшить стили для удаленных элементов:
   - Добавить более заметный фон (светло-красный)
   - Добавить зачеркивание текста для удаленных элементов
   - Улучшить существующие классы `.remove` и `.deletedItemStatus`
3. Показывать информацию о том, кто удалил и когда (из `adminSnapshot` и `createdAt`)
4. Добавить предупреждающий текст "Объект был удален"
5. Убедиться, что удаленные сущности отображаются во всех компонентах логов:
   - `AllProductsLogsTable.tsx`
   - `AllUsersLogsTable.tsx`
   - `AllDepartmentsLogsTable.tsx`
   - `AllBookingsLogsTable.tsx`
   - `AllServiceKitsLogsTable.tsx`
   - И другие компоненты логов

**Где можно посмотреть улучшения:**
- `/admin/product-management/products/logs` - логи товаров
- `/admin/users/logs` - логи пользователей
- `/admin/departments/logs` - логи отделов
- `/admin/bookings/logs` - логи записей
- `/admin/orders/logs` - логи заказов
- `/admin/booking-departments/logs` - логи адресов отделов записей
- `/admin/product-management/kits/logs` - логи комплектов ТО

---

## 🟡 Средний приоритет

### 4. Подключение API каталога запчастей
**Статус:** ✅ Выполнено

**Решение:**
- Товары в заказах сохраняются в таблице `OrderItem` с полными данными (название, SKU, цена, бренд, изображение)
- При удалении товара из базы данные товара остаются в заказе, так как они хранятся в `OrderItem`
- В логах заказов сохраняются полные снапшоты заказа, включая все товары из `orderItems`
- Данные товаров сохраняются в снапшотах логов, поэтому информация не теряется даже при удалении товара
- **Важно:** Логи также сохраняются в общую таблицу `ChangeLog`, поэтому информация остаётся даже если специализированные логи удалятся
- **Важно:** При удалении товара лог создаётся ДО удаления (в транзакции), чтобы он не удалился из-за Cascade

**Что было сделано:**
- Улучшены снапшоты заказов в логах - теперь включают полную информацию о товарах
- Создана функция `getFullOrderSnapshot()` для получения полного снапшота заказа со всеми связанными данными
- Добавлено логирование в общую таблицу `ChangeLog` при создании и изменении заказов
- Исправлено логирование при удалении товаров - теперь лог создаётся ДО удаления в транзакции
- Добавлено логирование в ChangeLog при удалении товаров (одиночное и массовое)

**Файлы, которые были изменены:**
- ✅ `src/lib/crossLogging.ts` - создана функция `getFullOrderSnapshot()`
- ✅ `src/app/api/orders/[orderId]/route.ts` - улучшены снапшоты в логах заказов, добавлено логирование в ChangeLog
- ✅ `src/app/api/orders/route.ts` - добавлено логирование в ChangeLog при создании заказа
- ✅ `src/app/api/products/[productId]/route.ts` - исправлено логирование при удалении товара (теперь ДО удаления), добавлено логирование в ChangeLog
- ✅ `src/app/api/products/bulk-delete/route.ts` - исправлено логирование при массовом удалении товаров (теперь ДО удаления), добавлено логирование в ChangeLog

---

### 4. Подключение API каталога запчастей
**Статус:** Запланировано

**Описание:** 
- Необходимо найти и подключить API с каталогом запчастей от сторонних поставщиков
- Нужно исследовать доступные каталоги запчастей и их API

**Что нужно сделать:**
1. Исследовать доступные каталоги запчастей и их API (например, AutoDoc, Exist и т.д.)
2. Выбрать подходящий каталог/каталоги
3. Изучить документацию API и требования к интеграции
4. Создать сервис для работы с внешним API
5. Реализовать синхронизацию данных с каталогом
6. Добавить функционал поиска товаров из внешнего каталога

**Файлы для создания/изменения:**
- `src/lib/catalog-api/` - новый сервис для работы с API каталога
- Возможно, новые таблицы в БД для хранения синхронизированных данных

---

### 5. Подключение онлайн видео-общения
**Статус:** Запланировано

**Описание:**
- Необходимо добавить функционал онлайн видео-общения между клиентом и администраторами
- Должно быть максимально удобно для пользователя (простое подключение, без установки дополнительного ПО)

**Что нужно сделать:**
1. Исследовать доступные решения (WebRTC, готовые сервисы типа Twilio Video, Jitsi Meet и т.д.)
2. Выбрать оптимальное решение (предпочтительно встроенное в браузер, без установки)
3. Реализовать интеграцию на клиентской части
4. Добавить панель управления видео-звонками в админ-панель
5. Создать систему уведомлений для администраторов о входящих звонках
6. Добавить возможность записи звонков (опционально)

**Файлы для создания/изменения:**
- `src/components/video-call/` - компоненты видео-общения
- `src/app/api/video-call/` - API для управления звонками
- `src/app/admin/video-calls/` - админ-панель для видео-звонков

---

### 6. Создать личный кабинет клиента
**Статус:** Запланировано

**Что нужно:**
- Страница `/profile` или `/account` для авторизованных клиентов
- Отображение списка заказов клиента
- Отображение списка записей (бронирований) клиента
- Возможность редактирования профиля (имя, фамилия, отчество)

**Файлы для создания:**
- `src/app/(site)/profile/page.tsx`
- `src/app/(site)/profile/ProfileContent.tsx`
- API endpoints для получения заказов и записей клиента

---

## 🟢 Низкий приоритет (можно доработать позже)

### 7. Проверка занятых слотов при бронировании
**Статус:** Можно доработать

**Описание:** Добавить проверку, не занято ли выбранное время перед созданием записи.  
**Примечание:** Не критично, так как все заявки обрабатываются менеджером вручную, менеджер ведёт учёт заявок НЕ на сайте.

---

## 📝 Заметки

- Все заявки на бронирование обрабатываются менеджерами вручную
- Менеджеры ведут учёт заявок НЕ на сайте
- Клиент выбирает отдел, заявка попадает в общие заявки отдела без назначенного менеджера
- Менеджеры потом берут заявки себе и присваивают их себе